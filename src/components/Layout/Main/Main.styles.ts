import styled from 'styled-components';
import theme from '../../../styles/theme';

const Main = styled.main`
  padding-left: ${theme.global.gutter};
  padding-right: ${theme.global.gutter};
  max-width: 1920px;
  width: 100%;
  margin: 0 auto;
`;

export { Main };
